<UserControl x:Class="TestManagementASM.Views.Student.StudentTestListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900">
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#1976D2" Padding="20">
            <TextBlock Text="BÀI THI CỦA TÔI"
                      FontSize="20"
                      FontWeight="Bold"
                      Foreground="White"/>
        </Border>

        <!-- Toolbar -->
        <Border Grid.Row="1" Background="#F5F5F5" Padding="15" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal">
                <Button Command="{Binding RefreshCommand}"
                       Width="35"
                       Height="35"
                       Background="White"
                       BorderBrush="#CCCCCC"
                       BorderThickness="1"
                       Cursor="Hand"
                       ToolTip="Làm mới">
                    <TextBlock Text="🔄" FontSize="16"/>
                </Button>
                
                <TextBlock Text="Danh sách bài thi có sẵn"
                          VerticalAlignment="Center"
                          Margin="15,0,0,0"
                          FontSize="14"
                          Foreground="#666666"/>
            </StackPanel>
        </Border>

        <!-- Data Grid -->
        <Border Grid.Row="2" Margin="15" BorderBrush="#E0E0E0" BorderThickness="1">
            <DataGrid ItemsSource="{Binding AvailableTests}"
                     SelectedItem="{Binding SelectedTest}"
                     AutoGenerateColumns="False"
                     IsReadOnly="True"
                     SelectionMode="Single"
                     CanUserAddRows="False"
                     CanUserDeleteRows="False"
                     CanUserResizeRows="False"
                     GridLinesVisibility="Horizontal"
                     HeadersVisibility="Column"
                     RowHeight="45"
                     FontSize="13"
                     Background="White"
                     BorderThickness="0">
                
                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#1976D2"/>
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="FontWeight" Value="Bold"/>
                        <Setter Property="Padding" Value="10"/>
                        <Setter Property="Height" Value="40"/>
                        <Setter Property="HorizontalContentAlignment" Value="Left"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>

                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Setter Property="Background" Value="White"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#E3F2FD"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#BBDEFB"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.RowStyle>

                <DataGrid.Columns>
                    <DataGridTextColumn Header="Tên bài thi" Binding="{Binding TestName}" Width="2*"/>
                    <DataGridTextColumn Header="Môn học" Binding="{Binding Class.Subject.SubjectName}" Width="*"/>
                    <DataGridTextColumn Header="Lớp" Binding="{Binding Class.ClassName}" Width="*"/>
                    <DataGridTextColumn Header="Thời gian (phút)" Binding="{Binding DurationMinutes}" Width="Auto"/>
                    <DataGridTextColumn Header="Số câu hỏi" Binding="{Binding TestQuestions.Count}" Width="Auto"/>
                    
                    <DataGridTemplateColumn Header="Thao tác" Width="Auto">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="Xem chi tiết"
                                       Command="{Binding DataContext.ViewTestDetailCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                       Width="100"
                                       Height="30"
                                       Background="#1976D2"
                                       Foreground="White"
                                       BorderThickness="0"
                                       Cursor="Hand"
                                       FontWeight="SemiBold">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="Button">
                                                        <Border Background="{TemplateBinding Background}"
                                                               CornerRadius="3"
                                                               Padding="10,5">
                                                            <ContentPresenter HorizontalAlignment="Center"
                                                                            VerticalAlignment="Center"/>
                                                        </Border>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#1565C0"/>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <!-- Loading Overlay -->
        <Border Grid.Row="0" Grid.RowSpan="3"
               Background="#80000000"
               Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="Đang tải..." FontSize="16" Foreground="White" FontWeight="Bold"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>

