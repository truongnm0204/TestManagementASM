<UserControl x:Class="TestManagementASM.Views.Teacher.TeacherClassListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900">
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0"
                Background="#1E88E5"
                Padding="20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="Lớp học của tôi"
                               FontSize="24"
                               FontWeight="Bold"
                               Foreground="White"/>
                    <TextBlock Text="Quản lý các lớp học bạn đang giảng dạy"
                               FontSize="13"
                               Foreground="#B3E5FC"
                               Margin="0,5,0,0"/>
                </StackPanel>

                <Button Grid.Column="1"
                        Command="{Binding RefreshCommand}"
                        Style="{StaticResource PrimaryButton}"
                        Background="White"
                        Foreground="#1E88E5"
                        Padding="15,10"
                        VerticalAlignment="Center">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="🔄" FontSize="14" Margin="0,0,5,0"/>
                        <TextBlock Text="Làm mới" FontSize="14"/>
                    </StackPanel>
                </Button>
            </Grid>
        </Border>

        <!-- Class List -->
        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding ManagedClasses}"
                  SelectedItem="{Binding SelectedClass}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  GridLinesVisibility="None"
                  HeadersVisibility="Column"
                  Margin="20"
                  Background="White"
                  BorderThickness="1"
                  BorderBrush="#E0E0E0">

            <DataGrid.Columns>
                <DataGridTextColumn Header="Tên lớp"
                                    Binding="{Binding ClassName}"
                                    Width="*"
                                    FontSize="14"
                                    FontWeight="SemiBold"/>

                <DataGridTextColumn Header="Môn học"
                                    Binding="{Binding Subject.SubjectName}"
                                    Width="*"
                                    FontSize="13"/>

                <DataGridTextColumn Header="Mã môn"
                                    Binding="{Binding Subject.SubjectCode}"
                                    Width="120"
                                    FontSize="13"/>

                <DataGridTextColumn Header="Học kỳ"
                                    Binding="{Binding Semester}"
                                    Width="100"
                                    FontSize="13"/>

                <DataGridTemplateColumn Header="Số sinh viên"
                                        Width="120">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Enrollments.Count}"
                                       FontSize="13"
                                       HorizontalAlignment="Center"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTemplateColumn Header="Số bài thi"
                                        Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Tests.Count}"
                                       FontSize="13"
                                       HorizontalAlignment="Center"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTemplateColumn Header="Thao tác"
                                        Width="150">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Command="{Binding DataContext.ViewClassDetailCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                    CommandParameter="{Binding}"
                                    Style="{StaticResource PrimaryButton}"
                                    Padding="10,5"
                                    FontSize="12">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="👁" Margin="0,0,5,0"/>
                                    <TextBlock Text="Xem chi tiết"/>
                                </StackPanel>
                            </Button>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>

            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#F5F5F5"/>
                    <Setter Property="Foreground" Value="#333333"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                    <Setter Property="Padding" Value="10"/>
                    <Setter Property="BorderThickness" Value="0,0,0,1"/>
                    <Setter Property="BorderBrush" Value="#E0E0E0"/>
                </Style>
            </DataGrid.ColumnHeaderStyle>
        </DataGrid>

        <!-- Loading Overlay -->
        <Border Grid.Row="0"
                Grid.RowSpan="2"
                Background="#80000000"
                Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
            <StackPanel HorizontalAlignment="Center"
                        VerticalAlignment="Center">
                <TextBlock Text="Đang tải..."
                           FontSize="16"
                           Foreground="White"
                           FontWeight="Bold"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>

