<UserControl x:Class="TestManagementASM.Views.Teacher.TeacherTestListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:TestManagementASM.Views.Teacher"
             mc:Ignorable="d"
             d:DesignHeight="600"
        d:DesignWidth="1000">
    <UserControl.Resources>
        <local:BoolToStatusConverter x:Key="BoolToStatusConverter"/>
        <local:BoolToColorConverter x:Key="BoolToColorConverter"/>
    </UserControl.Resources>

    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0"
                Background="#1E88E5"
                Padding="20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="Quản lý bài thi"
                               FontSize="24"
                               FontWeight="Bold"
                               Foreground="White"/>
                    <TextBlock Text="Tạo và quản lý các bài kiểm tra của bạn"
                               FontSize="13"
                               Foreground="#B3E5FC"
                               Margin="0,5,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="1"
                            Orientation="Horizontal"
                            VerticalAlignment="Center">
                    <Button Command="{Binding CreateTestCommand}"
                            Style="{StaticResource SuccessButton}"
                            Padding="15,10"
                            Margin="0,0,10,0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="➕"
                                    FontSize="14"
                                    Margin="0,0,5,0"/>
                            <TextBlock Text="Tạo bài thi mới"
                                    FontSize="14"/>
                        </StackPanel>
                    </Button>

                    <Button Command="{Binding RefreshCommand}"
                            Style="{StaticResource PrimaryButton}"
                            Background="White"
                            Foreground="#1E88E5"
                            Padding="15,10">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="🔄"
                                    FontSize="14"
                                    Margin="0,0,5,0"/>
                            <TextBlock Text="Làm mới"
                                    FontSize="14"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Test List -->
        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding TeacherTests}"
                  SelectedItem="{Binding SelectedTest}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  GridLinesVisibility="None"
                  HeadersVisibility="Column"
                  Margin="20"
                  Background="White"
                  BorderThickness="1"
                  BorderBrush="#E0E0E0">

            <DataGrid.Columns>
                <DataGridTextColumn Header="Tên bài thi"
                                    Binding="{Binding TestName}"
                                    Width="*"
                                    FontSize="14"
                                    FontWeight="SemiBold"/>

                <DataGridTextColumn Header="Lớp học"
                                    Binding="{Binding Class.ClassName}"
                                    Width="150"
                                    FontSize="13"/>

                <DataGridTextColumn Header="Môn học"
                                    Binding="{Binding Class.Subject.SubjectName}"
                                    Width="150"
                                    FontSize="13"/>

                <DataGridTemplateColumn Header="Số câu hỏi"
                                        Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding TestQuestions.Count}"
                                       FontSize="13"
                                       HorizontalAlignment="Center"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTextColumn Header="Thời gian (phút)"
                                    Binding="{Binding DurationMinutes}"
                                    Width="120"
                                    FontSize="13"/>

                <DataGridTemplateColumn Header="Trạng thái"
                                        Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border Background="{Binding IsActive, Converter={StaticResource BoolToColorConverter}}"
                                    CornerRadius="3"
                                    Padding="8,4">
                                <TextBlock Text="{Binding IsActive, Converter={StaticResource BoolToStatusConverter}}"
                                           FontSize="12"
                                           Foreground="White"
                                           HorizontalAlignment="Center"
                                           FontWeight="SemiBold"/>
                            </Border>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTextColumn Header="Ngày tạo"
                                    Binding="{Binding CreatedAt, StringFormat='dd/MM/yyyy'}"
                                    Width="100"
                                    FontSize="13"/>

                <DataGridTemplateColumn Header="Thao tác"
                                        Width="250">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal"
                                    HorizontalAlignment="Center">
                                <Button Command="{Binding DataContext.EditTestCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        Style="{StaticResource WarningButton}"
                                        Padding="8,4"
                                        Margin="2"
                                        FontSize="11">
                                    <TextBlock Text="✏ Sửa"/>
                                </Button>
                                <Button Command="{Binding DataContext.ToggleActiveCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        Style="{StaticResource PrimaryButton}"
                                        Padding="8,4"
                                        Margin="2"
                                        FontSize="11">
                                    <TextBlock Text="🔄 Đổi TT"/>
                                </Button>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>

            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background"
                            Value="#F5F5F5"/>
                    <Setter Property="Foreground"
                            Value="#333333"/>
                    <Setter Property="FontWeight"
                            Value="SemiBold"/>
                    <Setter Property="Padding"
                            Value="10"/>
                </Style>
            </DataGrid.ColumnHeaderStyle>
        </DataGrid>

        <!-- Loading Overlay -->
        <Border Grid.Row="0"
                Grid.RowSpan="2"
                Background="#80000000"
                Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
            <StackPanel HorizontalAlignment="Center"
                        VerticalAlignment="Center">
                <TextBlock Text="Đang tải..."
                           FontSize="16"
                           Foreground="White"
                           FontWeight="Bold"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>

