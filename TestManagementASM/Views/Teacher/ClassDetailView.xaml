<UserControl x:Class="TestManagementASM.Views.Teacher.ClassDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="700"
             d:DesignWidth="1000">
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0"
                Background="#1E88E5"
                Padding="20">
            <StackPanel Orientation="Horizontal">
                <Button Command="{Binding BackCommand}"
                        Width="35"
                        Height="35"
                        Background="#1565C0"
                        BorderThickness="0"
                        Cursor="Hand"
                        Margin="0,0,15,0">
                    <TextBlock Text="←"
                               FontSize="20"
                               Foreground="White"
                               FontWeight="Bold"/>
                </Button>
                <TextBlock Text="CHI TIẾT LỚP HỌC"
                           FontSize="20"
                           FontWeight="Bold"
                           Foreground="White"
                           VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- Content -->
        <ScrollViewer Grid.Row="1"
                      VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="30">
                <!-- Class Information Card -->
                <Border Background="White"
                        BorderBrush="#E0E0E0"
                        BorderThickness="1"
                        CornerRadius="8"
                        Padding="25"
                        Margin="0,0,0,20">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <TextBlock Text="{Binding CurrentClass.ClassName}"
                                       FontSize="24"
                                       FontWeight="Bold"
                                       Foreground="#1E88E5"
                                       Margin="0,0,0,10"/>

                            <TextBlock FontSize="14"
                                       Foreground="#666"
                                       Margin="0,0,0,5">
                                <Run Text="Môn học: "/>
                                <Run Text="{Binding CurrentClass.Subject.SubjectName}"
                                     FontWeight="SemiBold"/>
                            </TextBlock>

                            <TextBlock FontSize="14"
                                       Foreground="#666"
                                       Margin="0,0,0,5">
                                <Run Text="Số sinh viên: "/>
                                <Run Text="{Binding StudentScores.Count}"
                                     FontWeight="SemiBold"/>
                            </TextBlock>

                            <TextBlock FontSize="14"
                                       Foreground="#666">
                                <Run Text="Số bài thi: "/>
                                <Run Text="{Binding CurrentClass.Tests.Count}"
                                     FontWeight="SemiBold"/>
                            </TextBlock>
                        </StackPanel>

                        <Button Grid.Column="1"
                                Command="{Binding RefreshCommand}"
                                Style="{StaticResource PrimaryButton}"
                                Padding="15,10"
                                VerticalAlignment="Top">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="🔄"
                                           FontSize="14"
                                           Margin="0,0,5,0"/>
                                <TextBlock Text="Làm mới"
                                           FontSize="14"/>
                            </StackPanel>
                        </Button>
                    </Grid>
                </Border>

                <!-- Student Scores Table -->
                <Border Background="White"
                        BorderBrush="#E0E0E0"
                        BorderThickness="1"
                        CornerRadius="8"
                        Padding="20">
                    <StackPanel>
                        <TextBlock Text="Danh sách sinh viên và điểm"
                                   FontSize="18"
                                   FontWeight="Bold"
                                   Foreground="#1E88E5"
                                   Margin="0,0,0,15"/>

                        <!-- Table Header -->
                        <Border Background="#E3F2FD"
                                BorderBrush="#1E88E5"
                                BorderThickness="0,0,0,2"
                                Padding="15,10">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="3*"/>
                                    <ColumnDefinition Width="3*"/>
                                    <ColumnDefinition Width="1.5*"/>
                                    <ColumnDefinition Width="1.5*"/>
                                    <ColumnDefinition Width="1.5*"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0"
                                           Text="Tên sinh viên"
                                           FontWeight="Bold"
                                           Foreground="#1565C0"/>
                                <TextBlock Grid.Column="1"
                                           Text="Email"
                                           FontWeight="Bold"
                                           Foreground="#1565C0"/>
                                <TextBlock Grid.Column="2"
                                           Text="Điểm TB"
                                           FontWeight="Bold"
                                           Foreground="#1565C0"
                                           TextAlignment="Center"/>
                                <TextBlock Grid.Column="3"
                                           Text="Hoàn thành"
                                           FontWeight="Bold"
                                           Foreground="#1565C0"
                                           TextAlignment="Center"/>
                                <TextBlock Grid.Column="4"
                                           Text="Thao tác"
                                           FontWeight="Bold"
                                           Foreground="#1565C0"
                                           TextAlignment="Center"/>
                            </Grid>
                        </Border>

                        <!-- Table Content -->
                        <ScrollViewer MaxHeight="500"
                                      VerticalScrollBarVisibility="Auto">
                            <ItemsControl ItemsSource="{Binding StudentScores}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border BorderBrush="#E0E0E0"
                                                BorderThickness="0,0,0,1"
                                                Padding="15,12">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="3*"/>
                                                    <ColumnDefinition Width="3*"/>
                                                    <ColumnDefinition Width="1.5*"/>
                                                    <ColumnDefinition Width="1.5*"/>
                                                    <ColumnDefinition Width="1.5*"/>
                                                </Grid.ColumnDefinitions>

                                                <TextBlock Grid.Column="0"
                                                           Text="{Binding StudentName}"
                                                           FontWeight="SemiBold"
                                                           VerticalAlignment="Center"/>

                                                <TextBlock Grid.Column="1"
                                                           Text="{Binding Email}"
                                                           Foreground="#666"
                                                           VerticalAlignment="Center"/>

                                                <TextBlock Grid.Column="2"
                                                           TextAlignment="Center"
                                                           VerticalAlignment="Center">
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <Setter Property="Text"
                                                                    Value="{Binding AverageScore, StringFormat={}{0:F2}}"/>
                                                            <Setter Property="FontWeight"
                                                                    Value="Bold"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding AverageScore}"
                                                                             Value="0">
                                                                    <Setter Property="Foreground"
                                                                            Value="#999"/>
                                                                    <Setter Property="Text"
                                                                            Value="Chưa có"/>
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding AverageScore}">
                                                                    <DataTrigger.Value>
                                                                        <sys:Double xmlns:sys="clr-namespace:System;assembly=mscorlib">0</sys:Double>
                                                                    </DataTrigger.Value>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style>
                                                </TextBlock>

                                                <StackPanel Grid.Column="3"
                                                            Orientation="Horizontal"
                                                            HorizontalAlignment="Center"
                                                            VerticalAlignment="Center">
                                                    <TextBlock Text="{Binding CompletionStatus}"
                                                               FontWeight="SemiBold">
                                                        <TextBlock.Style>
                                                            <Style TargetType="TextBlock">
                                                                <Setter Property="Foreground"
                                                                        Value="#4CAF50"/>
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding CompletedTests}"
                                                                                 Value="0">
                                                                        <Setter Property="Foreground"
                                                                                Value="#999"/>
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </TextBlock.Style>
                                                    </TextBlock>
                                                </StackPanel>

                                                <Button Grid.Column="4"
                                                        Command="{Binding DataContext.ViewStudentDetailCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                        CommandParameter="{Binding}"
                                                        Style="{StaticResource SecondaryButton}"
                                                        Padding="10,5"
                                                        HorizontalAlignment="Center">
                                                    <TextBlock Text="Xem chi tiết"
                                                               FontSize="12"/>
                                                </Button>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>

                        <!-- Empty State -->
                        <TextBlock Text="Chưa có sinh viên nào trong lớp"
                                   FontStyle="Italic"
                                   Foreground="#999"
                                   HorizontalAlignment="Center"
                                   Margin="0,30,0,0"
                                   Visibility="{Binding StudentScores.Count, Converter={StaticResource ZeroToVisibilityConverter}}"/>
                    </StackPanel>
                </Border>

                <!-- Loading Indicator -->
                <Border Background="#80FFFFFF"
                        Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"
                        Padding="20"
                        Margin="0,20,0,0">
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock Text="Đang tải dữ liệu..."
                                   FontSize="16"
                                   FontWeight="Bold"
                                   Foreground="#1E88E5"
                                   HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>

