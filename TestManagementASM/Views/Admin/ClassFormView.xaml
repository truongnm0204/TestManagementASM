<UserControl x:Class="TestManagementASM.Views.Admin.ClassFormView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White">
    <Grid Margin="20">
        <StackPanel VerticalAlignment="Center" MaxWidth="400">
            <!-- Title -->
            <TextBlock Text="{Binding IsEditMode, StringFormat='Cập nhật lớp|Tạo lớp mới'}"
                       FontSize="18" FontWeight="Bold" Foreground="#333333" Margin="0,0,0,20"/>

            <!-- Error Message -->
            <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" TextWrapping="Wrap" Margin="0,0,0,10"
                       Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}"/>

            <!-- Class Name -->
            <TextBlock Text="Tên lớp:" FontSize="12" Foreground="#666666" Margin="0,0,0,5"/>
            <TextBox Text="{Binding Class.ClassName, UpdateSourceTrigger=PropertyChanged}"
                     Padding="10" FontSize="12" Margin="0,0,0,15"/>

            <!-- Subject -->
            <TextBlock Text="Môn học:" FontSize="12" Foreground="#666666" Margin="0,0,0,5"/>
            <ComboBox ItemsSource="{Binding Subjects}" SelectedValuePath="SubjectId" 
                      DisplayMemberPath="SubjectName" SelectedItem="{Binding SelectedSubject}"
                      Padding="10" FontSize="12" Margin="0,0,0,15"/>

            <!-- Semester -->
            <TextBlock Text="Kỳ học:" FontSize="12" Foreground="#666666" Margin="0,0,0,5"/>
            <TextBox Text="{Binding Class.Semester, UpdateSourceTrigger=PropertyChanged}"
                     Padding="10" FontSize="12" Margin="0,0,0,20"/>

            <!-- Buttons -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,0,0">
                <Button Command="{Binding SaveCommand}" Content="Lưu" 
                        Padding="15,8" Margin="0,0,10,0" Background="#4CAF50" Foreground="White" FontSize="12"/>
                <Button Command="{Binding CancelCommand}" Content="Hủy" 
                        Padding="15,8" Background="#f44336" Foreground="White" FontSize="12"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>

